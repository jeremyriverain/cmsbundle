{% extends('@GeekcoCms/base.html.twig') %}

{% block menu %}
    
{% endblock %}


{% block main %}

    <div class="section valign-wrapper">
        <div class="container">

            <a href="{{path('geekco_cms_index')}}" class="valign-wrapper"><i class="material-icons mr-1">home</i> Retour en page d'accueil</a>
            {{form_start(form)}}
            <div class="row">
                <div class="col s12 m8 offset-m2 center-align">
                    <h1 class="">Nouveau mot de passe</h1>
                </div>

                <div class="input-field col s12 m8 offset-m2 l6 offset-l3">
                    {{form_row(form.plainPassword.first)}}
                </div>

                <div class="input-field col s12 m8 offset-m2 l6 offset-l3">
                    {{form_row(form.plainPassword.second)}}

                    <button class="btn waves-effect waves-light"  type="submit">Envoyer</button>
                </div>


                {{form_end(form)}}

            </div>
        </div>
{% endblock %}
